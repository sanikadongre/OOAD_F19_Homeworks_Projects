package src;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

//Unused as of now
public class PlayerMove extends Hand {
   private final static Logger logger = LogManager.getLogger(PlayerMove.class);
}
